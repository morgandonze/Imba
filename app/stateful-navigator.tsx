import rootNavigator from "./root-navigator";

import { createAppContainer } from "react-navigation";
import RootNavigator from "./root-navigator";

const StatefulNavigator = (props: any) => {
  return <RootNavigator />;
};

export default StatefulNavigator;
